Retry the same fully validated final grouped-media consistency hardening after fixing only the workflow cleanup before rebase. Functional requirements are unchanged: one user submission is one logical post; up to 10 images and 5 videos stay inside one card; count once for list, pagination, new-post checks, optimistic counters and comment totals; keep actual video-file totals; isolate groups by board+author+parent; apply matching D1 indexes; preserve existing data. Re-run the complete regression suite and dependency audit before committing.
