Apply the final guarded grouped-media consistency hardening: one user submission must remain one logical post even when it contains multiple files. Keep up to 10 images and 5 videos inside one post card; count it once in list, pagination, new-post checks, optimistic browser counters, total/today post counts and reactions/replies; keep actual video-file totals; isolate media groups by board+author+parent even if a UUID is maliciously reused; add matching D1 indexes; preserve existing data and schema compatibility. Run the complete regression suite and dependency audit before committing verified functional changes.
